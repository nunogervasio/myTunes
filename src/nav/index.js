import ResponsiveMenu from "./responsiveMenu";
import NavMenu from "./navMenu";

export { NavMenu };

/**
 * To run this nav the following dependences are needed:
 * yarn add react-icons
 * yarn add styled-components
 */
